[allowlist]
  description = "Global allowlist — Claude local settings contain tool permission patterns, not real secrets"
  paths = [
    '''apps/\.claude/settings\.local\.json''',
    '''\.claude/settings\.local\.json''',
  ]
